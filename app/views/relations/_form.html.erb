<%= form_with(model: relation) do |form| %>
  <% if relation.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(relation.errors.count, "error") %> prohibited this relation from being saved:</h2>

      <ul>
        <% relation.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :main_card_id, style: "display: block" %>
    <%= form.number_field :main_card_id %>
  </div>

  <div>
    <%= form.label :sub_card_id, style: "display: block" %>
    <%= form.number_field :sub_card_id %>
  </div>

  <div>
    <%= form.label :point, style: "display: block" %>
    <%= form.number_field :point %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
